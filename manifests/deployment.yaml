apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-reservation
  namespace: restaurant-reservation
  labels:
    app: restaurant-reservation
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-reservation
  template:
    metadata:
      labels:
        app: restaurant-reservation
    spec:
      nodeSelector:
        "kubernetes.io/os": linux
      containers:
      - name: k8s-restaurant-reservation-argocd
        image: k8s-restaurant-reservation-argocd
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - name: rust-app
          containerPort: 8000


